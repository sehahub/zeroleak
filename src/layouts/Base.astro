---
import '../styles/global.css';

interface Props {
  title: string;
  description: string;
  path?: string;
}
const { title, description, path = '/' } = Astro.props;
const site = Astro.site ?? new URL('https://zeroleak.sehahub.info');
const canonical = new URL(path, site).href;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    <meta name="description" content={description} />
    <link rel="canonical" href={canonical} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:type" content="website" />
    <meta property="og:url" content={canonical} />
    <meta property="og:site_name" content="ZeroLeak" />
    <meta property="og:image" content={new URL('/og.png', site).href} />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta property="og:image:alt" content="ZeroLeak — a black redaction box beside the text recovered from underneath it" />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:image" content={new URL('/og.png', site).href} />
    <link
      rel="icon"
      href="data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 32 32'%3E%3Crect width='32' height='32' rx='7' fill='%2314161a'/%3E%3Crect x='7' y='9' width='18' height='4.5' rx='1' fill='%23c0261c'/%3E%3Crect x='7' y='16' width='13' height='2.6' rx='1' fill='%23f4f3f0'/%3E%3Crect x='7' y='21' width='16' height='2.6' rx='1' fill='%23f4f3f0'/%3E%3C/svg%3E"
    />
  </head>
  <body>
    <header class="site-head">
      <div class="wrap">
        <a class="brand" href="/"><span class="dot"></span>ZeroLeak</a>
        <nav>
          <a href="/#checks">What it checks</a>
          <a href="/research/government-pdfs">Research</a>
          <a href="/guides">Guides</a>
          <a href="/#privacy">Privacy</a>
          <a href="/#faq">FAQ</a>
        </nav>
      </div>
    </header>

    <main>
      <slot />
    </main>

    <footer>
      <div class="wrap">
        <span>ZeroLeak — PDF leak scanning that never uploads your file.</span>
        <nav>
          <a href="/">Scanner</a>
          <a href="/research/government-pdfs">Research</a>
          <a href="/guides">Guides</a>
          <a href="/#privacy">Privacy</a>
          <a href="/#faq">FAQ</a>
        </nav>
      </div>
    </footer>
  </body>
</html>
